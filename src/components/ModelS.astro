---
import Section from "./Section.astro"
---



<Section 
title="Model S"
subtitle="Programe una consulta virtual"
color="black"
id="model-s"

>
			
<div slot="footer" class="flex gap-x-4 mx-auto">
    <a class="border-[3px] border-gray-600 bg-gray-600 text-sm rounded font-medium text-white px-12 py-2 inline-block transition-colors hover:border-gray-500 hover:bg-gray-500 " href="#">Pedido personalizado</a>

    <a class="bg-white text-sm rounded font-medium px-12 py-2 hover:bg-black hover:text-white transition-colors inline-flex justify-center items-center text-black " href="#">Saber mas</a>
</div>

    
<img 
slot="background"
src="/model-s.avif" 
alt="Model S" 
class="h-full w-full object-cover object-center"
id="bg-image-modely">

</Section>

<script>
    function isEdge() {
      if (
        navigator.userAgent.indexOf("Edge") > -1 ||
        navigator.userAgent.indexOf("Edg") > -1
      ) {
        return true;
      } else {
        return false;
      }
    }
  
    let imageElement = document.querySelector("#bg-image-models") as HTMLImageElement; // TypeScript
  
    if (isEdge()) {
      imageElement.src = "model-s.webp";
    } else {
      imageElement.src = "model-s.avif";
    }
  </script>